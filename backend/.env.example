APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=sqlite
#DB_HOST=mysql
#DB_PORT=3306
#DB_DATABASE=predictive_patterns
#DB_USERNAME=systemAdmin
#DB_PASSWORD=Q1e2§3r4t

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

FILESYSTEM_DISK=local
CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

# Redis Database Separation
REDIS_QUEUE_DB=2
REDIS_CACHE_DB=1
REDIS_SESSION_DB=3

# Queue Configuration
QUEUE_CONNECTION=redis
TRAINING_QUEUE_DRIVER=redis
TRAINING_QUEUE=training
TRAINING_QUEUE_RETRY_AFTER=1800
TRAINING_QUEUE_TIMEOUT=3600
TRAINING_QUEUE_CONNECTION=null

# Broadcasting
BROADCAST_DRIVER=pusher
BROADCAST_FALLBACK_ENABLED=true
BROADCAST_FALLBACK_CONNECTION=log

# Horizon Configuration
HORIZON_TRAINING_CONNECTION=training
HORIZON_TRAINING_MAX_PROCESSES=4

# Pusher / Reverb Configuration
PUSHER_APP_ID=predictive-patterns
PUSHER_APP_KEY=local-key
PUSHER_APP_SECRET=local-secret
PUSHER_HOST=localhost
PUSHER_PORT=6001
PUSHER_SCHEME=http
