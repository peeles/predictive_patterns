export const acceptedTypes = Object.freeze([
    'text/csv',
    'application/vnd.ms-excel',
    'application/json',
    'text/plain',
    'application/csv',
    'text/comma-separated-values',
]);

export const csvMimeTypes = Object.freeze([
    'text/csv',
    'application/vnd.ms-excel',
    'application/csv',
    'text/comma-separated-values',
    'text/plain',
]);
